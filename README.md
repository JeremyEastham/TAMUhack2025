# Install
```shell
python -m venv .venv

# Linux & MacOS
source .venv/bin/activate
# Windows
.\.venv\Scripts\activate

pip install -r requirements.txt
```

# Run Backend
```shell
python -m tamuhack
```